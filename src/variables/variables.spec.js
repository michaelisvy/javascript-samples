const variables = require("./variables");

describe('Name of the group', () => {
    test('should ', () => {
        variables.functionA();
        const result = variables.functionB(1);
        expect(result).toEqual(2);
        console.log(variables.numberA);
    });
});